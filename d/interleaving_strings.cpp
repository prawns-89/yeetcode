/**
 * Problem: Check if given string is interleaving of two other given strings
 * 
 * Note: This problem is already present in the original YeetCode/LeetCode database.
 * To avoid redundant code generation, please refer to the original question.
 * 
 * LeetCode Link: https://leetcode.com/problems/interleaving-string
 * YeetCode Slug: interleaving-string
 */

#include <iostream>
#include <string>
#include <vector>

int main() {
    std::cout << "This problem is already present in the original database." << std::endl;
    std::cout << "LeetCode URL: https://leetcode.com/problems/interleaving-string" << std::endl;
    std::cout << "Slug: interleaving-string" << std::endl;
    return 0;
}
